//InitialState
const InitialState = {};

//Action types

//Action creators

//Reducer function
export default function(state = InitialState, action){
    switch(action.type){
        default:
            return state
    }
}